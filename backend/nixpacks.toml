[phases.setup]
nixPkgs = ["php82", "php82Extensions.pdo", "php82Extensions.pdo_mysql", "php82Extensions.mysqli"]

[phases.install]
cmds = ["echo 'No composer needed'"]

[start]
cmd = "php -S 0.0.0.0:${PORT:-8080} -t ."
